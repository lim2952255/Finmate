# Docker가 Spring 이미지를 빌드할 때 전송하지 않을 파일 목록이다.
# 빌드에 불필요한 로컬 도구·캐시·산출물을 제외한다.
.git
.github
.gradle
.idea
.omx
.codex

# 비밀번호와 API 키가 Docker 이미지에 들어가지 않게 한다.
.env
.env.*

# 기존 빌드 결과 대신 컨테이너 안에서 JAR를 새로 만든다.
build

# Spring 프로젝트용 이미지는 백엔드 전용이므로 프론트엔드는 포함하지 않는다.
frontend
summary
*.iml
*.log
